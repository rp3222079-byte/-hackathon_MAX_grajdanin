
├── .gitignore             
├── Makefile                 
├── README.md 


backend/                       # весь Python-бэкенд
│
├── .env                       # реальные секреты: пароли, ключи, DATABASE_URL — НЕ коммитится
├── .env.example                # шаблон .env без значений — коммитится, ориентир для команды
│
│
├── parser/                    # код парсинга трёх источников (сайтминжкх + сторонний сайт эл./воды);
│                               #   ожидаются файлы запросов к сайтам и разбора HTML в данные
│
│
├── database/                  # модели таблиц, подключение к БД, миграции/схема;
│                               #   сам файл .db создаётся здесь при работе, но в git не коммитится
│
│
├── chatbot/                   # логика чат-бота: обработка сообщений пользователя,
│                               #   формирование ответов на основе данных из database/
│
│
├── api/                       # HTTP-эндпоинты (Flask/FastAPI), которые дёргает frontend;
│                               #   мост между chatbot/database и React-приложением
│
│
├── main.py                    # точка входа бэкенда — здесь сервер запускается (make run-backend)
├── pyproject.toml             # конфиг ruff (форматирование + линтинг Python)
└── requirements.txt           # список Python-зависимостей, ставится через make install
│
│
docs/                          # всё, что не код: ТЗ от тимлида, схемы архитектуры/БД, рабочие заметки
│
│
frontend/                      # весь React-фронтенд
│
├── .env                       # реальный адрес API и т.п. — НЕ коммитится
├── .env.example                # шаблон — коммитится
│
│
├── .eslintrc.json              # правила поиска ошибок в JS/JSX
├── .prettierrc                  # правила форматирования JS/JSX/CSS
├── package.json                  # зависимости и npm-скрипты (npm start, npm build)
│
│
├── public/                    # статика (index.html, favicon) — отдаётся как есть, не через сборщик
│
│
└── src/
    ├── App.jsx                # корневой компонент, роутинг между страницами
    │
    │
    ├── components/             # переиспользуемые кусочки UI — ожидаются файлы кнопок, карточек,
    │                            #   виджета чат-бота и т.п.
    │
    │
    └── pages/                   # цельные экраны приложения — ожидаются файлы отдельных страниц
                                  #   (например страница чата, страница дашборда показаний)